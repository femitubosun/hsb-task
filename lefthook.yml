pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts}"
      run: pnpm lint
    format:
      glob: "*.{js,ts}"
      run: pnpm format
      stage_fixed: true

pre-push:
  commands:
    test:
      run: pnpm test
